[pytest]
filterwarnings =
    ignore::DeprecationWarning
addopts = -vvv -l --alluredir=report --strict-markers
log_level = info
log_format = %(asctime)s [%(levelname)s] %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
